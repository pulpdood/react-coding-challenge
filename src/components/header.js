import React from 'react';

import '../styles.css';

function Header(props) {
  const [shows, setShows] = React.useState([]);
  return <header className="header">DEMO SCREENING</header>;
}

export default Header;
